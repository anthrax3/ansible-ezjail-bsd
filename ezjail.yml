---
- hosts: jails
  become_user: root
  become: true
  roles:
    - bootstrap
    - jailmaster 
    - flavor
  pre_tasks:
    - name: debug some variables
      debug:
        var: "{{ item }}"
      with_items:
        - ronald
        - masterserver_ip
        - masterserver_interface
        - jails_ip_block
