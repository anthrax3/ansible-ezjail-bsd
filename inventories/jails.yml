# This is a template for an inventory which can be used to configure a
# "localhost" - add localhost to one (and only one) of the groups below
# If you want to add more roles than one, be careful to not add conflicting roles

[local-machines]
localhost
# user interfaces

[jails]
192.168.33.10 ansible_user=vagrant
